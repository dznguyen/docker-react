sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t dznguyen/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true dznguyen/docker-react npm run test -- --coverage
